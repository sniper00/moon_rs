[package]
name = "lib-common-sys"
version = "0.1.0"
edition = "2021"
links = "common"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
doctest = false

[dependencies]
libc = "0.2"

[build-dependencies]
cc = "1.0"
